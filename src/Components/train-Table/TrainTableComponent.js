function TrainTableComponent(props) {
    return (
        <div data-testid="train-1">{props.jwt}</div>
    )

}

 export const functionForTesting = (a,b) => {
     return ( a + b);
 }

export default TrainTableComponent;